.env
venv/
.venv/
__pycache__/
*.pyc
elasticsearch_snapshots/
flask_session/
data/
indexes/
*.log
es_bulk_errors.json

*.pem
*_credentials.csv
# NOTE: FAISS index files are committed intentionally. Rebuild scripts depend on their presence.

# Actual story files are omitted from repo, but placeholders are kept for testing.
faiss_index.bin
summaries_index.faiss
index_to_filename.json

# Contains the actual contents that populate the ES instances.  A few were left in the placeholder version to showcase.
summaries.json
questions-answers.json